# THEME

The Arkira theme system: design tokens, Tailwind v4 `@theme`, and shadcn/ui
semantic tokens, with a built-in responsive and mobile layer.

- `theme-standard.md`: the canonical standard.
- Reference implementation: `examples/theme-reference/`.
